# glowframe
A fullscreen white-light web page that turns any display into a soft front-facing fill light for video recording, calls, and selfies.

## Documentation

- [REQUIREMENTS.md](REQUIREMENTS.md) — Business Requirements Document (BRD) with full feature specifications and implementation checklist.

## Deployment / Release

GlowFrame is hosted on **GitHub Pages** and served directly from the `docs/` folder on the `main` branch. There is no CI pipeline — releases are done manually.

**Two build commands:**
- `npm run build` — compiles to `dist/` (local dev and testing only, does **not** touch `docs/`).
- `npm run build:prod` — production release build. Automatically runs `npm run build` first (via `prebuild:prod`), then copies `dist/` to `docs/` and creates `docs/.nojekyll`.

1. **Bump the version:** Use npm to increment `package.json` version before building.  
   ```bash
   npm version patch   # or: minor | major | <explicit semver>
   ```
   This updates `package.json` (and `package-lock.json`) and creates a git version tag automatically.

2. **Build for release:** `npm run build:prod`  
   This compiles the app to `dist/`, copies it to `docs/`, and creates `docs/.nojekyll` to prevent GitHub Pages from running Jekyll.

3. **Commit:** Stage and commit the updated `docs/` folder together with the version bump.  
   ```bash
   git add docs/ package.json package-lock.json
   git commit -m "chore: release build"
   git push && git push --tags
   ```

3. **Configure GitHub Pages** (one-time setup):  
   In the repository's **Settings → Pages**, set the source to the `main` branch and the folder to `/docs`.

**Custom domain:** To use a custom domain, place a `CNAME` file containing your domain inside `docs/` before committing (e.g., `echo "example.com" > docs/CNAME`). The `CNAME` file is not generated by the build and must be added manually each time or preserved across builds.

**Changing the base path:** If the repository is renamed or moved to a root-level custom domain, update `base` in `vite.config.ts` (e.g., `base: '/'`) and rebuild. No other source files need changing.

